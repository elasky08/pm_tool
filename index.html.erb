resources :product


def index
  @product = Product.all
end

def new
  @product = Product.new
end

def create
@product = Prduct.new
if @product.save require(:product).permit(:title, :body, :price)
  redirect_to product_path(:id), notice: "hooray"
  else
  flash[:alert] = "wrong"
  render :new
  end
end

def edit
  @product = Product.find params[:id]
end

def update
  product = Product.find params[:id]
  if product.update require(:product).permit(:title,:body,:price)
  redirect_to products_path, notice: "hooray"
  else
  flash[:alert] = "wrong"
  render :edit
  end
end

def destroy
  @product = Product.find params[:id]
  @product.destroy
  redirect_to products_path
end

def show 
  @product = Product.find params[:id]
end

show page
@product.each do |f|
f.title
f.body
f.price
end


index.html.erb
@product.each do |f|
f.title
f.body
f.price
end

new.html.erb
form_for @product do |f|
f.text_field :title
f.text_area :body
f.text_field :price
f.submit
end
