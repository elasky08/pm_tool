<h1>Edit Task</h1>

<%= @task.errors.full_messages.join(", ") %>
<%= form_for @task do |task| %>
  <div>
    <%= task.label :title %>
    <%= task.text_field :title %>
  </div>
  <%= task.submit %>
<% end  %>
